import 'package:flutter/material.dart';

class ThemeColors{
  final Color _primaryColor = Color.fromRGBO(255, 75, 0, 1);
  final Color _accentColor = Color.fromRGBO(0, 0, 0, 1);
  final Color _supportColor = Color.fromRGBO(243, 244, 245, 1);
  final Color _borderColor = Color.fromRGBO(228, 229, 229, 1);
  final Color _primarySuport= Color.fromRGBO(50, 50, 50, 0.5);

  Color get primaryColor => _primaryColor;

  Color get accentColor => _accentColor;

  Color get supportColor => _supportColor;

  Color get borderColor => _borderColor;

  Color get primarySuport => _primarySuport;
}